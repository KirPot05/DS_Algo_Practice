#include <iostream>
#include "number.h"

using namespace std;

int main(int argc, char const *argv[])
{

    // Negative numbers conversion not handled
    cout << "Decimal to Bindary of 10: " << decBin(-10) << endl;

    // cout << "Binary to decimal of 1100: " << binDec(1100) << endl;

    // cout << "Reverse of 123: " << reverse(4500) << endl;

    return 0;
}
